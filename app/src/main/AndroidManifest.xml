<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apusart.got_android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/got_pttk"
        android:label="@string/app_name"
        android:roundIcon="@drawable/got_pttk"
        android:supportsRtl="true"
        android:theme="@style/LoggedTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.manage_segment.ManageSegmentActivity" />
        <activity android:name=".ui.join_trip.JoinTripActivity" />
        <activity android:name=".ui.add_segment.AddSegmentActivity" />
        <activity android:name=".ui.tourist_profile.TouristProfileActivity"/>
    </application>

</manifest>